{
    "require": {
        "symfony/http-foundation": "^5.1"
    },

    "scripts": {
        "serve": "php -S localhost:8080 -t ./public"
    },

    "config": {
        "process-timeout": 0
    }
}
